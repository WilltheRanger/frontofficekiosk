# Supabase — dbhs-wayfinder project (shared with the interactive map app).
# These two values are PUBLIC BY DESIGN: they ship inside the kiosk JS bundle.
# Security comes from Row Level Security (anon = SELECT-only, live rows only) —
# see PLANNING.md §4.3 / §11.1 and supabase/migrations/.
VITE_SUPABASE_URL=https://mnvntttootxbbbnsnvke.supabase.co
VITE_SUPABASE_KEY=sb_publishable_TLnFfdFCkX0_NoLFenvEqA_MGqjWmqV

# Without these vars the app runs in mock mode (placeholder photos, sample
# announcements and rooms) — useful for offline development and demos.
